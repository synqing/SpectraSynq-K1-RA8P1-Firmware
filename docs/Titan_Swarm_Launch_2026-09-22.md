# Start here — Titan parallel execution pack

This pack changes execution scheduling, not the application architecture or acceptance scope. It has not been installed or launched on the Mac. No hardware action is authorized by opening it.

## Use the replacement plan

Open `Titan_Parallel_Swarm_Execution_2026-09-22.plan.md`. It replaces the serial queue. `sources/` preserves the supplied plan and separately records newer read-only Mac observations. Reconcile the current receipts rather than treating the older attachment as present target identity.

The `agents/` directory contains six engineering role definitions, one integrator, one exclusive bench operator and one read-only independent verifier. Use them through supported native Cursor background tasks after the coordinator verifies paths, permissions and actual runtime capacity. Do not blindly copy/overwrite existing agent configuration files. `task_graph.json` is a dependency planning appendix, not a running scheduler or a second status ledger. The templates are unadmitted examples with no runnable hardware command.

## Paste into the current Cursor parent

Finish or checkpoint the current operation safely. Do not kill a process or disturb the target to restart this work.

Adopt the attached Titan Parallel Swarm Execution plan as the scheduling replacement. Preserve its original acceptance crosswalk and all current no-emit, wiring, source and explicit hardware-action boundaries. Reconcile the newer STATUS.md/AGENTS.md/E2 Lite router and actual receipts first; do not repeat the old candidate deployment or known hanging GDB attach merely because the prior list still names them.

Become coordinator C. Assign one source/integration owner I and one exclusive Titan/E2 Lite/CDC owner H. Preserve the dirty working tree, seal the applicable source/dependency snapshot and prepare private staging with exact nonoverlapping edit ownership. No new sibling product repository/worktree, global permission relaxation or cloud upload.

Launch D, X, A, V, M and P concurrently using their scoped role briefs. Do not wait for T1 and do not await each worker before launching the next. Use actual background task handles and respect available concurrency/quota. Every lane implements, tests and resolves its own in-scope defects; it sends complete patches to I and complete experiment packets to H. Workers never contact the device.

I integrates the next useful ready subset without waiting for all lanes. H executes only complete, authorized, identity-bound packets through the currently qualified mechanism, captures once and fans sealed evidence out to the relevant analysts. Keep intrusive/debug/profiler/observer-off/physical conditions separate. Host work continues during hardware or operator waits.

Return actual agent/task handles, integrated source/candidate identities, passing and failing tests, sealed measurements, preserved first faults, target leave-state and precise remaining gates. Do not return another plan-only checkpoint, fake parallelism with shell placeholders, or close full T1 from emit-disabled/host evidence.

## First dispatches

D: inspect and repair debug/deployment host tooling. X: bind six tests and fix pair/capture defects. A: sample/ASRC/time invariants and host injections. V: behavior/parity, then wide-colour in the same owner lane. M: shared real fixture and usable MIR/control workflow. P: capture/scorer contracts, Q1–Q5 readiness and concurrent evidence analysis.

The first useful integrated cut does not wait for all of those to finish. A blocked wiring or debug row does not become a global barrier.
