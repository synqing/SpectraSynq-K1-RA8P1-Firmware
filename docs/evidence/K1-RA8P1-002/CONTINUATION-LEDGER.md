---
abstract: "K1-RA8P1-002 continuation dispositions, reconciled 2026-09-16 against retained build/application receipts. Historical entries preserved. Not a flash grant or new live identity query."
---

# Continuation ledger — 2026-09-13

## Current reconciliation — 2026-09-16

**Priority override — colour integrity.** Captain has paused onwards development
for the frozen EdgeAI colour packet. The resident completion waiver credits
unfinished DMA with zero errors: earlier zero-error receipts are not a clean
transmission verdict. The real guard, immutable opcode-22/v2 witness, safe staged
global DMA initialisation and matched fixed/round-robin ARM candidates are now
implemented, host/build tested and physically run. Both failed with the same
one-word terminal underrun. See
[TITAN-COLOUR-INTEGRATION.md](TITAN-COLOUR-INTEGRATION.md) for exact images,
commands, negative tests and the numbered remaining silicon/wire closure path.
This takes precedence over the onwards sequence below until resolved.

**Subsequent live execution.** On Captain's Rearm, the second waiter programmed
fixed build `d9700b14…`; the runner independently identified it and retained
frame 3468 with one remaining DMA word at timer stop. It credited no false
success, latched one DMA fault, restored settings and released CDC. A subsequent
Rearm programmed round-robin build `49c582db…`; that run failed at frame 2063
with the same one-word remainder and likewise restored/released cleanly. The
round-robin image was then superseded by LED-first build `a3f37e8a…` on the same
UID. The LED-first run failed at frame 2030 with the same one-word remainder,
and likewise restored/released cleanly. That image is now resident with its
fault latched. The linked DMA configuration proves channels 0/1/2 despite stale
literal 0/1 PDM channel labels in its platform JSON. A source-only telemetry
correction cross-compiles but is not resident. No wire/optical cure or external
closure is claimed.

This section supersedes the implementation blockers in the historical table
below; it does not overwrite failed campaigns or close external tickets.
Lead development/integration ownership follows Captain's latest request.
Two source-bound colour candidates were flashed and run. No room playback or
commit was performed.

Pre-colour baseline retained for comparison, **not current resident**: build
`1594115df349d5cfbd3d1adbc418c095b008b860ebc4d11954ff2cc11d893693`,
UID `545433931bd25436593630352d068363`, pinned source `6b1e7bc5…`,
`sr24000.hop180.bins80.xover40`, U55 closed. The firmware checkout remains
`lane/k1-ra8p1-002` at `c2d4dca` plus unpublished changes.

Build/programme/run root:
`/Users/spectrasynq/Workspace_Management/EdgeAI_Artifacts/Titan/k1-ra8p1-002`.
Build `pdm-ap-gpt-build-20260915-09/receipt.json` has 97 source hashes;
93 remain unchanged. Four deliberate colour-integration deltas are the GPT
driver C/header, opcode branch and staging script; both new builds bind all 97
current hashes. The baseline HEX SHA-256 equals
`pdm-ap-gpt-prog-20260916-01/receipt.json`. The programmer receipt alone
does not verify application identity: its separate `identity.json` and run-06
do. This is retained evidence, not a fresh resident query. The current resident
is the failed LED-first build identified above.

| Ticket / area | What is implemented or observed now | What remains before closure |
|---|---|---|
| R0/R1 | Source/build/application chain reconciled; existing combined contract retained | Preserve this exact checkpoint and distinguish historical G4 from combined qualification |
| D1/D2 / colour integrity | Guard/v2 witness retained three real one-word terminal underruns; fixed, round-robin and LED-first all failed without false completion | Same-frame P601/DIN pulse/bit/reset comparison, measured repair and repeated controls; visible-corruption issue stays open |
| D3/D4 | Output treatment and TRUE16 packer retained; current capture uses 128-pixel, 24-bit bench profile | Native two-channel/WS2816 wiring and output qualification remain separate |
| E.2–E.4 | Dual PDM, 40 kHz configuration and measured-rate ASRC are implemented and in the identified build; historical measured hop-rate admission is retained | Capsule acoustic identity, negative as well as positive raw rails, sustained capture and applicable configuration/timing proof |
| Q1–Q3 / D5 | Combined image exists. Named real music run-06 responds and returns to black; zero send errors and no added positive rails in that window | Motion quality, worst-case render/AP cost, releases/jitter, under-8-ms end-to-end timing and a justified combined workload campaign |
| Quiet-gap close-out path | Missing-data/short-duration false-PASS prevention and runner RMS/frame validation repaired; 28 HOST tests pass | One exclusive-owner real-acoustic capture through the repaired runner, source-bound frame/PCM/counter evidence and declared five-second quiet-and-black tail |
| SS-02 / SS-03 | SS-02 accepted evidence retained; SS-03 live4 demonstrates dashboard progress, recording and handoff | Actual Historian replay, mandatory local counters, restart stale-to-fresh recovery and current source/project binding; `ok:true` is not full closure |
| H1/W3 / frontend | Existing host feature adapter/probes retained; no new model or taxonomy | Separate musical injection from propagation/decay using retained real-music input; verify occupancy and motion, then song-held-out visual utility; preserve canonical frontend expectations |
| T / K1-L/L2 / J1 / K1-C | No new physical or production qualification claimed | Applicable latency traces, power/transport proof and Captain's production ruling remain outstanding |

Run-01's original `TRUE_BLACK` result used a generated click. It remains
historical and is never replayed under `NO_SYNTHETIC_ROOM_AUDIO`. An offline
rescore through the repaired gate preserves five seconds of sampled black
(9.040–14.040 s) and fade 3.425 s from firmware `live_age_us`. All 115 retained
480-byte frame records independently match their JSON summaries and hashes.
Run-06 used `Regard_Ride_It.mp3` for twenty seconds: hop maximum 13,585,
LED maximum 70, final nonzero count zero, zero send errors. A changing centroid
does not establish the intended travelling motion when the occupied half fills.

### Lead-owned onwards sequence

1. Agent: isolate music-presence, injection, propagation and decay with retained
   real-music input and scored native frames. Change only the first demonstrated
   runtime failure; protect centre origin, palette safety, no heap and dt-correct
   recovery. No further gain/chroma threshold experiments by default.
2. Agent: qualify the repaired close-out runner with real sound under a single
   explicitly transferred CDC owner. Captain provides `Rearm` only if a changed
   image actually needs programming. No synthetic room audio or same-song loop.
3. Agent: finish SS-03 actual replay and fresh recovery against its current
   manifest, retaining accepted components rather than restarting SS-02.
4. Agent: establish combined deadline, release and end-to-end latency evidence
   without inheriting the frozen scalar G4 result. Resolve only measured failures.
5. Agent: capture the physical wire/receiving-path and capsule evidence using
   appropriate instruments. Captain supplies genuinely unavailable hardware or
   recording choices, not routine diagnosis or buffer scoring.
6. Agent: reconcile exact acceptance receipts and reproducibility. Captain alone
   grants commits and production ruling/flash. Product closure is the existing
   K1-C/G8 decision bound to the exact accepted build, not a HOST test count.

The following table remains a **historical 2026-09-13 snapshot**. Unmentioned
areas retain their existing authority/disposition; this update does not reopen
cadence, cache-on, M33 or model work.

Authority: firmware `AGENTS.md`, `TITAN-MIC-LED2-SESSION-CANON.md`, this STATUS
index, `combined-runtime-contract.json`. No commit unless Captain asks.

| Ticket | Disposition | Evidence / remaining |
|---|---|---|
| R0 | HOST index updated | STATUS.md; closed G4 scalar PASS scoped; LED2/mic historical |
| R1 | HOST contract written | `combined-runtime-contract.json`; OSPI vs PDM DMAC0 conflict named |
| S1 | REUSED + slim snapshot | Existing reducer/patterns; `k1_status_led_slim_snapshot` |
| S2 | HOST implemented | Budgeted Clause-22 frames; no auto experiment; RGB independent of USB hold |
| S3 | HOST implemented | Opcode 20 slim/busy while schedule active |
| S4 | DEFERRED_DEPENDENCY | Needs identified combined image + Rearm |
| D1 | DEFERRED_DEPENDENCY | GPT/DMA source present; last silicon zero completions; resume `run_gpt_dma_autonomous.py` on a fresh programme dir |
| D2 | DEFERRED_DEPENDENCY | 128-px WS2812 control strip; needs D1 |
| D3 | HOST implemented | DualMCU blend/edge/gain/joint-limit ported; `scripts/test_output_composition.py` |
| D4 | DEFERRED_DEPENDENCY | TRUE16 packer retained; WS2816 profile and extra lanes need wiring |
| D5 | OPEN | Combined workload after D/Q |
| E.1 | DEFERRED_DEPENDENCY | Run-02 transport preserved; needs controlled acoustic observation |
| E.2 | HOST partial | Rearm/FIFO repairs retained; 30 s runner still bounded 0.25–10 s |
| E.3 | OPEN | 40 kHz candidate not generated/read back |
| E.4 | HOST implemented | `k1_asrc_24k` 36–44 kHz → 24 kHz/180; not live-admitted |
| E.5–E.7 | OPEN | Need E.1–E.4 + operator |
| Q1–Q3 | OPEN | Combined image not built this session |
| T/K1-L/L2 | OPEN | Trace fields not yet in GPT completion ISR |
| K1-B/K1-A | DEFERRED_DEPENDENCY | Comparator authority read; production S3 mutation still false |
| J1 | OPEN | No power instrument this session |
| C / W5 / B | NOT_NEEDED unless measured bottleneck | Cache-off retained; cache-on USB fail quarantined |
| I1 | OPEN inventory | CMSIS used only as selected later |
| H1 | HOST adapter | `feature_strength_shape`; musical holdout `DEFERRED_DEPENDENCY` |
| W3 | DEFERRED_DEPENDENCY | Needs H1 consumer on one named centre-origin effect |
| W4 | NOT_NEEDED pending inspection | No second histogram stacked; G4 tempo retained |
| H2/H3/F/WP15 | G6 `NO_QUALIFYING_CANDIDATE` preserved | No replacement-model search |
| G / M33 | NOT_NEEDED | No measured service split |
| I2/I3 | DEFERRED_DEPENDENCY | No authorised recording/Colab job selected |
| X1 | REUSED donors | `DONOR-FILE-LIST.md`; remaining GitHub rows stay reference-only |
| G8 / K1-C | OPEN | Captain owns production ruling |

Resume physical: prepare a named diagnostic image, waiter command, fresh
programme directory, then wait for Captain `Rearm`. Do not query the board
while another owner may hold CDC.

**Document Changelog**
| Date | Author | Change |
|------|--------|--------|
| 2026-09-13 | agent:grok | Created from continuation execution; host S/D/E/H1 plus R0/R1. |
