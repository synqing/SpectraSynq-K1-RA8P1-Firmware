# 001 ↔ 002 evidence map — later paper

Not the current Titan gate.

Issue stamps only for silicon already proved on a named UID and build. Do not
back-stamp G4, LED2, or microphone results onto a different resident image.

The live colour-integrity fault on `a3f37e8a…` (one DMA word remaining) is
closed by P601/DIN capture of that retained frame, not by this map.
