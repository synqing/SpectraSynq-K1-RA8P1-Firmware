# Live-runtime candidate waiter — hold

Named ARM candidate: `live-k1-runtime-build-20260920-02`. Physical emit defaults **off**. This is not the live-audio-gpt return image and not a transmitter admission.

On Rearm for **this** candidate: start `COMMAND.sh` first. First chat line is exactly `WAITING`. Relay programmer events from `events.jsonl`. Do not invent `PROGRAMME_VERIFY_PASS`. Never reuse the programme or run directories.

After WRITE_VERIFIED: release/reset once if Captain has not already done it, then `RUNNER.sh`. The runner binds HEX/ELF/build-id and does not claim photons.

CDC: recover ownership before target access. A dead PID proves a stale lock, not a completed `lsof` check.

Phase 8 recording, replay and same-input comparison remains scheduled after development acceptance. Full feature-coverage closure remains required for a completed-port claim.

Hash verification of these artefacts is integrity only. Runtime and transmitter each need their own tests.
