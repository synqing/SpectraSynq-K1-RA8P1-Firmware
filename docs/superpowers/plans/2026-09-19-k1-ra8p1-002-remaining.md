# Programme B — RA8P1-002 Titan lane (2026-09-19)

Lane F (DualMCU music/presence/radio soak) is a **separate programme** in the
DualMCU tree. This file is the Titan continuation only.

## Critical chain (approved, then Rearm overrode preserve-resident)

preserve unpublished source → P601/DIN capture of retained `a3f37e8a` frame
was required before any other image → first physical divergence → repair only
that → full-frame wire/guard agreement → uncapped profiler flash → RA8P1 vs
RT1062 ruling.

Captain Rearm 2026-09-19 wrote profiler `32dd1f5f…` over `a3f37e8a…` before
`WAVEFORM_CAPTURED`. That preserve-resident step is closed as waived, not as
captured. Do not stamp `WAVEFORM_CAPTURED` from host/compile-only.

## Already done

- Stale `titan_broker` stopped; CDC locks released.
- 1.8 GB `broker.jsonl` hashed and moved outside git. Not truncated.
  SHA-256 `ba47ecba0bf61a6b3aae8218e9cfac0e637bd3d143efb60b8b75d4fd752d478d`.
- Predicted 3072-bit / 3070-DMA reference from retained frame 2030, plus a
  fail-closed scorer. Missing capture, historical WS2812 diagnostic, short
  synthetic, and synthetic `WAVEFORM_CAPTURED` overclaim all fail.
- Uncapped profiler cross-built to `g4-uncapped-raw-hops-build-20260919-02`
  (`32dd1f5f…`). Captain Rearm programmed it WRITE_VERIFIED. Overlay order is
  stage-profile then DTCM.
- Autostart: live WaveformK1 mode 32, flags 5 (active+emit, no carousel).
  Host palette suite PASS 2026-09-19. Morph live-audio pixels are allowed to
  differ from pinned (Captain: different).
- Live-audio image built, not on silicon (ROM waiters timed out).

## Capture protocol

[docs/evidence/K1-RA8P1-002/P601-DIN-CAPTURE-PROTOCOL.md](../../evidence/K1-RA8P1-002/P601-DIN-CAPTURE-PROTOCOL.md)

Predicted wire: 128 × 24 = 3072 bits. DMA count `bits-2` = 3070. Pin P601 =
GTIOC6A. Probe MCU pad and first LED DIN.

## Uncapped profiler

Source instrument is behind `K1_ENABLE_STAGE_PROBE`: raw hops, stage timers,
and a delay mutation (hop 136 on a 6000-hop fixture; hop 0 on the 1-hop host
fixture). Cross-build `32dd1f5f…` was programmed on Captain Rearm 2026-09-19.
`WAVEFORM_CAPTURED` is still absent. Ruling stays deferred until a physical
P601/DIN capture exists.

## Ruling

[docs/RA8P1-vs-RT1062-RULING.md](../../RA8P1-vs-RT1062-RULING.md) remains
deferred until uncapped Q2 exists.
